exports.signup = (req, res, next) => {
  console.log(req.body);
  // Does email exist?

  // If exist, return error

  // If does not exist: create and save user record

  // Respond to request indicates the user is created
};
